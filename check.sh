echo "🔍 Проверка выполнения заданий..."

# Проверка Эпизода 1
if [ -f "fixed-Dockerfile" ]; then
    echo "✅ Эпизод 1: Dockerfile исправлен"
else
    echo "❌ Эпизод 1: Нет исправленного Dockerfile"
fi

# Проверка Эпизода 2
if docker ps -a | grep -q "vulnerable-app"; then
    echo "✅ Эпизод 2: Контейнер запускался"
else
    echo "❌ Эпизод 2: Контейнер не запускался"
fi